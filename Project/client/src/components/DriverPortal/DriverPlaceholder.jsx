import React from 'react';

const DriverPlaceholder = ({ title }) => {
    return (
        <div className="placeholder-container">
            <h2>{title}</h2>
            <p>This module is currently under construction and will be available in a future sprint.</p>
        </div>
    );
};

export default DriverPlaceholder;
